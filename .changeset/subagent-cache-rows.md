---
'@inbrace-tech/tokenline': minor
---

Subagent rows in Claude Code's agent panel, each with its own cache countdown and the tool it waits on; `init` wires `subagentStatusLine` (`--no-subagents` to skip).
